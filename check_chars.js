const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const courses = await prisma.course.findMany({where: {id: {in: [52, 53, 54]}}}); courses.forEach(c => { console.log('ID:', c.id); const s = c.link_anh_bia || ''; for(let i=0; i<s.length; i++) { process.stdout.write(s.charCodeAt(i) + ' '); } console.log(''); }); } main().finally(() => prisma.$disconnect());
